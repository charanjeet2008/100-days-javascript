const FiniteQueue = require("./Hash");
const assert = require('assert');


HashTest = {};

HashTest['Hash.pull should return Error on empty queue'] = function(done){
    // var Q = new FiniteQueue(5);
    // var val = Q.pull();
    // assert(val instanceof Error);
    // assert.equal(val.statusCode, 404);
    // assert.equal(val, "Error: Not Found");
    done();
};
